package com.android.fmradio

import android.app.Application

/**
 * The Application class. Currently does not need to perform any specific initialization.
 */
class FMRadioApplication : Application() {
    // No custom initialization logic needed at this time.
    // The FileLogger is now a simple logcat wrapper and doesn't need to be initialized.
}
